query getAccountClosureStatement(
    $accountId: String!
) {
    getAccountClosureStatement(
        accountId: $accountId
    ) {
        expiresAt,
        contractInfo {
            ...AccountClosureStatementInfoFragment
        }
    }
}
