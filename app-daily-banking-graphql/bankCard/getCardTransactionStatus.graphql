query getCardTransactionStatus (
    $transactionId: String!
) {
    getCardTransactionStatus (
        transactionId: $transactionId
    ) {
        result
        error {
            ...UserTransactionErrorFragment
        }
    }
}
