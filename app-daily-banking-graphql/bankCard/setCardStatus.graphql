mutation setCardStatus (
    $cardStatus: CardStatus!,
    $cardToken: String!
) {
    setCardStatus (
        cardStatus: $cardStatus,
        cardToken: $cardToken
    ) {
        transactionId
    }
}
