query renewSession (
    $otp: String!,
    $phoneNumber: String!,
    $deviceId: String!
) {
    renewSession (
        otp: $otp,
        phoneNumber: $phoneNumber,
        deviceId: $deviceId
    ) {
        ...TokenFragment
    }
}

