mutation prepareToken(
    $timestamp: String!,
    $device: String!,
    $deviceId: String!
) {
    prepareToken(
        timestamp: $timestamp,
        device: $device,
        deviceId: $deviceId
  )
}
